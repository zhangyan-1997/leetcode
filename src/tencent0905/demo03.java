package tencent0905;

import java.util.Scanner;
/*
public class demo03 {
    static int max = 0;
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = Integer.parseInt(sc.nextLine());
        String s = sc.nextLine();
        maxValue(s, );
    }

    private static void maxValue(String s, int nextVal, int index, int sum){
        if(index == s.length()) {
            max = Math.max(sum, max);
            return;
        }
        if(index>0 && s.charAt(index) == s.charAt(index-1))  maxValue(s, nextVal+1, index+1, sum + nextVal);
        if(index>0 && s.charAt(index) == s.charAt(index-1)){
           maxValue(s, 1, index+1, sum + nextVal);
        }
    }
}

 */
